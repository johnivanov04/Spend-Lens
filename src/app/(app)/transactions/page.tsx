import { SectionPlaceholder } from "@/components/section-placeholder";

export default function TransactionsPage() {
  return (
    <SectionPlaceholder
      title="Transactions"
      description="Add, review, and correct transactions across manual entry, receipt paste, and CSV upload."
      phase="Phase 3 (input) & Phase 5 (review table)"
    />
  );
}
